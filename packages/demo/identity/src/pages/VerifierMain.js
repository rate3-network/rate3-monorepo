import React from 'react';
import PropTypes from 'prop-types';
import { withStyles } from '@material-ui/core/styles';

const styles = (theme) => {
  return ({

  });
};
const VerifierMain = (props) => {
  const { classes } = props;
  return (
    <div>
      
    </div>
  );
};

VerifierMain.propTypes = {
  
};

export default withStyles(styles)(VerifierMain);
